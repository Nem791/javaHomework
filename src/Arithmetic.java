import java.util.Random;

public class Arithmetic {
    static void ClassMath() {
        System.out.println("So pi: " + Math.PI);
        System.out.println("So pi: " + Math.ceil(5.6));

        Random random = new Random();
        System.out.println(random.nextInt(98));
    }
}
